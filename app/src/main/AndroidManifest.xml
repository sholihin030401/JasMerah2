<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.ichwan.gemastik">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".admin.DaftarAdminActivity"></activity>
        <activity android:name=".admin.RegistAdminActivity" />
        <activity android:name=".admin.LoginAdminActivity" />
        <activity android:name=".user.UserActivity" />
        <activity android:name=".user.RegistUserActivity" />
        <activity android:name=".user.LoginUserActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>